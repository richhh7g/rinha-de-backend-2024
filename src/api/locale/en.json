{
  "error": {
    "server-runner": {
      "running": "Server closed."
    }
  },
  "success": {
    "server-runner": {
      "running": "The server is running on http://{{ host }}:{{ port }}."
    }
  }
}
